export const session = {
  /**
   * Secret use by polka for his session
   */
  secret: null,

  /**
   * Store use by polka for saving his session
   */
  store: null,

  /**
   * Extras options pass to polka session middleware
   */
  options: {}
}
